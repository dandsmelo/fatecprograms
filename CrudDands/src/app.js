//Padrão
const express = require('express')//importação do express
const app = express() //instância do express
const port = 3000 //porta para o projeto local

//Rota padrão ou raiz
app.get('/', (req, res) => {

 )}